# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npx pnpm i --no-frozen-lockfile --strict-peer-dependencies=false && npm run docs:build:update
      - uses: MajesticString/action-hosting-deploy@v0.6
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DISCORD_SNIPER_5C7F0 }}'
          channelId: live
          projectId: discord-sniper-5c7f0
